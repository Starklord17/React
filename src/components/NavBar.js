import CartWidget from "./CartWidget";

const NavBar = () => {
    return (
        <div className="NavBar">
            <h1>Carrito</h1>
            <CartWidget />
        </div>
    );
};

export default NavBar;

