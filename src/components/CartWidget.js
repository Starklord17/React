import cartLogo from "../img/cart64.png";

const CartWidget = () => {
    console.log(CartWidget);
    return (
        <div>
            <img src={cartLogo} alt='Cart'></img>
        </div>
    );
};

export default CartWidget;